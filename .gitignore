.DS_Store
.claude/

# Resident roster exports contain PII (names + emails) — keep out of git.
# The address lookup only needs room numbers, which are embedded in the app.
*.xlsx

# Screen recordings — large, and only ever used for sharing in the moment.
*.mp4
*.MP4
*.mov
*.MOV

# Working docs kept local
AGENTS.md
USEFULL_BrandGuide.md
